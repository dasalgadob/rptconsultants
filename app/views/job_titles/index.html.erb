<%= render 'layouts/breadcrumb', titles: [['Menu',menu_path], ['Companies',companies_path],[@company.name,company_path(@company)]]%>
<h1>Cargos <a href="#" id="tutorial"><i class="fas fa-question-circle fa-xs icon-blue-question" ></i></a></h1>

<table class="table table-hover" data-step="2" data-intro="Desde esta tabla los cargos ya creados usted los puede ver, editar o eliminar. " data-position="top">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Area</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @job_titles.each do |job_title| %>
      <tr>
        <td><%= job_title.name %></td>
        <td><%= get_job_title_area_name(job_title) %></td>
        <td><%= link_to 'Ver', job_title %></td>
        <td><%= link_to 'Editar', edit_job_title_path(job_title) %></td>
        <td><%= link_to 'Eliminar', job_title, method: :delete, data: { confirm: 'Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<a href="<%= new_company_job_title_path %>" data-step="1" data-intro="Para crear sus cargos le da clic a la opción “ Nuevo cargo”, y procede a escribir el nombre de su cargo, y para asignarle el área o Unidad de Negocio, el sistema automáticamente le despliegan la lista con  las opciones de estas que fueron creadas en los pasos anteriores. " data-position="top">Nuevo cargo</a>

