<%= render 'layouts/breadcrumb', titles: [['Menu',menu_path], ['Empresas', companies_path], [@area.company.name, company_path(@area.company)],['Areas', company_areas_path(@area.company)]]%>
<p id="notice"><%= notice %></p>

<p>
  <strong>Nombre de Área:</strong>
  <%= @area.name %>
  <a href="#" id="tutorial"><i class="fas fa-question-circle fa-lg icon-blue-question" ></i></a>
</p>

<p>
  <strong>Unidad de negocio:</strong>
  <%= get_area_business_unit_string(@area) %>
</p>

<%= link_to 'Editar', edit_area_path(@area) %> |
<%= link_to 'Volver', company_areas_path(@area.company) %>


<%= render 'job_titles/index', job_titles: @area.job_titles %>
