<%= render 'layouts/breadcrumb', titles: [['Menu',menu_path]]%>
<h1>Editing Area</h1>

<%= render 'form', area: @area %>

<%= link_to 'Show', area_path(@area) %> |
<%= link_to 'Back', company_areas_path(@area.company) %>
