<%= form_for(role) do |f| %>
  <% if role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role.errors.count, "error", plurar="errores") %> impidieron a este rol de ser guardado:</h2>

      <ul>
      <% role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label "Nombre" %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= f.label "Grado" %>
    <%= f.text_field :degree_id, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= f.label "Tipo de posición" %>
    <%= f.text_field :position_type_id, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit locals[:submit_string], class: 'btn btn-success'%>
  </div>
<% end %>
