<p id="notice"><%= notice %></p>
<%= render 'layouts/breadcrumb', titles: [['Menu',menu_path], ['Tablas de referencia', references_tables_path]]%>

<h1>Criteria</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Tipo de criterio</th>
      <th>Puntaje</th>
      <th>Grado</th>
      <th>Descripción</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @criteria.each do |criterium| %>
      <tr>
        <td><%= criterium.criteria_type %></td>
        <td><%= criterium.score %></td>
        <td><%= criterium.degree.number %></td>
        <td><%= criterium.description %></td>
        <td><%= link_to 'Show', criterium %></td>
        <td><%= link_to 'Edit', edit_criterium_path(criterium) %></td>
        <td><%= link_to 'Destroy', criterium, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Criterium', new_criterium_path %>
